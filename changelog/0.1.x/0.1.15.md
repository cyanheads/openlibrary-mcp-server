---
summary: "Run the release:github script under bun instead of tsx, which is not an installed dependency."
breaking: false
security: false
---

# 0.1.15 — 2026-06-15

## Fixed

- **`release:github` script** — now runs `scripts/release-github.ts` via `bun run` instead of `tsx`. `tsx` is not a project dependency, so the script failed to execute under Bun.
